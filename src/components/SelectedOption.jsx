import { useState } from "react"

export const SelectedOption = (machineopt) => {
    
  const [selected, setselected] = useState({})

  return (
    <>{/* 
        <div className={machineopt==='tijeras' ? 'tijeras':'piedra'}>
        </div> */}
    </>
  )
}
